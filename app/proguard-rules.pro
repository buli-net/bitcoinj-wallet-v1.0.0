# Release documentation: application-specific ProGuard/R8 rules.
# The current release does not require additional project rules beyond the default
# Android optimization configuration referenced by app/build.gradle.
