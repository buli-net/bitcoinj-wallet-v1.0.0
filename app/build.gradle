apply plugin: 'com.android.application'

android {
    compileSdkVersion 28
    buildToolsVersion "28.0.3"

    defaultConfig {
        applicationId "wallet.main"

        minSdkVersion 26
        targetSdkVersion 28

        versionCode 1
        versionName "1.0"

        multiDexEnabled true
        vectorDrawables.useSupportLibrary = true

        javaCompileOptions {
            annotationProcessorOptions {
                arguments = [
                    androidManifestFile: "$projectDir/src/main/AndroidManifest.xml".toString()
                ]
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'),
                    'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

configurations.all {
    exclude group: 'com.google.code.findbugs', module: 'jsr305'
    exclude group: 'com.google.code.findbugs', module: 'annotations'
    exclude group: 'net.jcip', module: 'jcip-annotations'

    resolutionStrategy {
        force 'com.google.guava:guava:27.0.1-jre'
        force 'org.slf4j:slf4j-api:1.7.36'
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    // Release dependency group: Android Support Library components used by the legacy UI.
    implementation 'com.android.support:appcompat-v7:28.0.0'
    implementation 'com.android.support:design:28.0.0'
    implementation 'com.android.support:support-v4:28.0.0'
    implementation 'com.android.support:recyclerview-v7:28.0.0'

    // Release dependency group: AndroidAnnotations generates the Activity binding classes.
    implementation 'org.androidannotations:androidannotations-api:4.6.0'
    annotationProcessor 'org.androidannotations:androidannotations:4.6.0'

    // Release dependency group: ZXing provides QR scanning.
    implementation('com.journeyapps:zxing-android-embedded:3.6.0') {
        exclude group: 'com.google.code.findbugs'
        exclude group: 'net.jcip'
        exclude group: 'com.google.guava'
    }

    implementation 'com.google.zxing:core:3.4.1'

    // Release dependency group: QRGen generates the receive-address QR bitmap.
    implementation 'com.github.kenglxn.QRGen:android:2.6.0'

    // Release dependency group: bitcoinj provides the Bitcoin wallet and blockchain core.
    implementation('org.bitcoinj:bitcoinj-core:0.17.1') {
        exclude group: 'com.google.code.findbugs'
        exclude group: 'net.jcip'
        exclude group: 'com.google.guava'
        exclude group: 'org.slf4j'
    }

    // Release runtime dependency required by bitcoinj 0.17.1.
    implementation 'com.google.protobuf:protobuf-javalite:4.33.4'

    // Release dependency group: Guava version aligned with the bitcoinj application baseline.
    implementation 'com.google.guava:guava:27.0.1-jre'

    // Release dependency group: Android logging bridge used by the Bitcoin stack.
    implementation 'org.slf4j:slf4j-android:1.7.36'
}
